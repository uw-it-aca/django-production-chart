{{- if not .Values.services }}
{{- if .Values.service.enabled }}
{{- $name := ( include "django-production-chart.releaseIdentifier" . ) }}
{{- include "django-production-chart.specService" (dict "root" . "name" $name "service" .Values.service ) }}
{{- end }}
{{- end }}
